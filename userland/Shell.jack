class Shell {
    function void main() {
        var char c, key;
    	var String s;
    	var boolean ok;
    
        while (true) {
            
            let s = Keyboard.readLine("$ ");

            if (String.compare(s,"pong") = 0) {
                do Output.printString("YES!");
                do PongMain.main();
            } else {
                do Output.printString("NOO!");
            }

            do Output.println();
	    }
        return;
    }
}
