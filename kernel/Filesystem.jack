class Filesystem {
    function void init() {
        var int diskChar;
        let diskChar = Disk.peek(0);
        //do Output.printInt(diskChar);
        //do Output.println();
        return;
    }
}
